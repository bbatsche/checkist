<%# These items will need to be broken down into their correct views and partials. %>
<%# For now though, we'll just use these static partials for layout purposes. %>
<%= render 'pages/incoming_todos'%>
<%= render 'pages/existing_todos'%>
<%= render 'pages/completed_todos'%>
